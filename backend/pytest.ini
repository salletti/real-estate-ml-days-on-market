[pytest]
# Dossier racine des tests
testpaths = tests

# Affichage verbeux par défaut : montre chaque test individuellement
addopts = -v

# Rapport de couverture : quels fichiers on mesure
# (désactivé ici, activé à la demande avec --cov)
